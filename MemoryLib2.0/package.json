{
  "name": "memoria",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n client,server,worker -c blue,green,magenta \"npm run dev -w client\" \"npm run dev -w server\" \"npm run worker -w server\"",
    "build": "npm run build -w client && npm run build -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
